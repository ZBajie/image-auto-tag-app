import { Routes } from '@angular/router';
import { ImageAutoTagComponent } from './pages/image-auto-tag/image-auto-tag.component';

export const routes: Routes = [
  {
    path: '',
    component: ImageAutoTagComponent,
  },
];
